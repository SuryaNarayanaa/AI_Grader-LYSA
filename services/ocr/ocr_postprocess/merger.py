import re

def merge_text_and_formulas(text, formula_text):
    """
    Merge regular text with detected formulas.
    And group by the questions.
    
    Args:
        text (str): Regular text from OCR
        formula_text (str): Extracted formula text
        
    Returns:
        str: Combined text with formulas properly integrated
    """    
    return 


def clean_merged_text(text):
    """
    Clean up merged text to ensure proper formatting.
    
    Args:
        text (str): Text with merged formulas
        
    Returns:
        str: Cleaned text
    """    
    
    return